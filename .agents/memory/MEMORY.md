- [Sales dashboard = static HTML](sales-dashboard-app.md) — artifacts/sales-dashboard renders from a self-contained index.html, NOT the React scaffold; edit index.html; its sales totals are legit (per-month counts), unlike iss-task-pro MTD.
- [Hierarchy visibility](hierarchy-visibility.md) — ISS Task Pro data-scoping client UX via buildHierarchySet; real server auth+scope added June 2026 (see server-auth.md); boss credential-viewer intentional.
- [Server auth & scope](server-auth.md) — signed httpOnly cookie session (no DB store), requireAuth gates all /api except health+/auth; passwords NOT hashed on purpose; scope EVERY sensitive read (tasks/:id+aggregates, credentials, notifications); DEV-only.
- [All-centers viewer / Director](hierarchy-visibility.md) — `isAllCentersViewer(u)` (MIS||Director dept) = every viewer `isMis` flag; Director is a dept (not role) to dodge Management→Boss trap; leave target-user MIS predicates alone. As of June 2026 MIS/Director DO see Head Office TASKS on TaskList/Dashboard/Reports/Team + server scopeTasks; still HO-excluded on AssignmentMonitor/EOD/credentials/centerPermissions-ceiling + all write-restrictions.
- [Email via Resend](email-resend.md) — ISS Task Pro email uses Resend (RESEND_API_KEY), not SMTP; test-mode only sends to account-owner email until domain verified; FROM_EMAIL sets sender.
- [In-app notifications](notifications.md) — bell+toast polling; notify assignee on assign & giver on done-transition (guard prev-status to avoid dup); now session-scoped+owner-enforced server-side (see server-auth.md).
- [Background web push](notifications.md) — popup toggle also manages a Web Push sub (tab-closed popups); ALL notification inserts must use createNotifications() or push is skipped; VAPID keys in app_settings, never rotate.
- [Theme-aware status colors](theme-status-colors.md) — hardcoded light-only Tailwind status colors (bg-red-50 etc.) need dark: variants or they go invisible on the 8 dark themes; semantic tokens already adapt.
- [Email rate limit](email-resend.md) — Resend free tier = 2 req/sec; sendEmail retries 429 w/ backoff + digest loop paced ~600ms; 9AM cron only fires if app is published (always-on).
- [Recurring task reset](recurring-tasks.md) — daily/weekly/monthly/quarterly/annual auto-clone per period (parentTaskId chain + lastRunDate, IST cron); per-task `dueRule` recomputes each occurrence's deadline; MUST run on boot; partial-unique-index idempotency.
- [Weekend skip for daily tasks](recurring-tasks.md) — Sun=all off, 1st Sat=all off, other Sat=HO-non-MIS off; daily gen skips off-days; self-serve "Generate Today's Tasks" rows use autoGenerated=false so boot cleanup spares them.
- [Dashboard date filter & analytics](dashboard-analytics.md) — date filter + all timeline charts bucket by dueDate||createdAt over a period-derived window; never updatedAt/fixed rolling windows.
- [Attendance & EOD](attendance-eod.md) — EOD viewers incl. Team Leaders; /api/eod GET+POST server-scoped (TL=own row/center) — frontend roles MUST match server gate or 403; shrinkage derived; upsert (submittedBy,date).
- [Center filter](center-filter.md) — users.center groups by office (default Head Office); tasks have NO center column, scoped via userId→center map; boss-only Dashboard pill row.
- [Center filter convention](center-filter.md) — boss-only Center filter on Dashboard/Team/TaskList/AssignmentMonitor; tasks have no center column so center derived via assignee userId→center map; ordered Head Office→Thane→Malad→Pune→Navi Mumbai; hidden when ≤1 center.
- [Assign Task picker scoping](assign-task-scoping.md) — rule = named Sets in shared lib `@workspace/assign-scope` (CODE not DB); since July 2026 ALSO server-enforced on GET /users for non-elevated viewers (assign scope ∪ own subtree) so old mobile APKs obey it too.
- [Task assigner default](task-assigner.md) — bulk-added JD/routine tasks are the person's OWN (assignedBy=NULL); don't show the boss as assigner.
- [TaskList filters](daily-lock-and-tasklist-filters.md) — All AND Daily tabs show all dates (past+future visible); "My Tasks" toggle; daily past-due LOCK was REMOVED July 2026 on user request — don't re-add.
- [Sales MTD](sales-mtd.md) — per-(user,month) self-entered month-to-date sales; amount is OVERWRITTEN daily not summed (summing inflates); no grand total; Head Office only for Boss+MIS-dept; partial-field upsert.
- [ISS Sales Report page](iss-sales-report-page.md) — iss-task-pro `/sales-report` Recharts dashboard over Agent Tracking metrics (Sales MTD/Target/DC/Prospect/Sales FD); client window-filter + Team.tsx roll-up rules; Boss/MIS all agents, CH/TL subtree; NOT the standalone sales-dashboard artifact.
- [Duplicate users & dashboard filter](duplicate-users-dashboard.md) — same person can exist twice (TL-with-login+0-tasks vs Sales-Agent-no-login+tasks); member pills show the empty TL; confirm before merging.
- [MyTasks future-date default](daily-lock-and-tasklist-filters.md) — MyTasks now defaults to dateMode "all" + sorts by dueDate asc, so future-due tasks show immediately (was today-only range hiding them).
- [Compliance Calendar](compliance-calendar.md) — Accounts statutory tracker; period-based due dates; gated Boss/MIS/Director see all, non-overseers see+tick own org-chart subtree (self + reports via buildHierarchySet), server-enforced read+write.
- [Assign-list visibility](assign-list-visibility.md) — users.assignable toggles who shows in Assign Task picker (login/status untouched); Boss/MIS/Director-only, gated FIELD-LEVEL on PUT /users/:id (shared endpoint); assignee options show center.
- [Access Control](access-control.md) — per-user `pagePermissions` (string[]|null) overrides role-default page visibility; Boss/MIS-only `/access` page; `always`/`system` markers prevent lockout/escalation; UI gate only (data endpoints keep own scoping).
- [Per-user center access](center-permissions.md) — centerPermissions restricts a Boss/MIS viewer to chosen centers; restriction-only (never widens role; MIS never Head Office; null=role default).
- [Compliance↔Tasks mirror](compliance-tasks-mirror.md) — Compliance Calendar activities auto-generated as real tasks (per item per period, idempotent index); task type=item frequency (recurring generator MUST skip complianceItemId tasks); visible to Accounts+MIS+Boss only; two-way completion sync via complianceSync.ts writing DB directly; dates in @workspace/compliance-dates lib. Company master list (`/compliance/companies` GET/POST/DELETE) is PUBLIC (above the compliance gate) so every user can add/remove companies in the task Company picker (mirrors category management).
- [Task edit/do permissions & transfers](task-edit-permissions.md) — assigner/elevated EDIT vs assignee status-only (status/remark-only PUT exception for the reason flow); transfers scoped via scopeTasks (NO seesAll bypass); manual reminder triggers must be admin-gated; due reminders = dueDate==IST+2 & !done, 10AM+3PM.
- [Global logout](global-logout.md) — admin "force everyone to re-login": cookie carries `<uid>.<epoch>`, DB global epoch (app_settings); bump = all stale, re-issue caller; Boss/MIS only.
- [Lunch reminders](lunch-reminders.md) — the "order by 12 / lunch at 1" nudges are a dashboard banner + in-app bell (NO email, NO tasks); boot retireLunchTasks() deletes old joke tasks so never re-create those exact titles; needs 2 extra cron-job.org triggers.
- [Categories & Company scoping](categories-company-scoping.md) — categories center-scoped, unique per (center,name), server-enforced; Company picker HO-only UI gate; no client dup pre-check (rely on 409).
- [Agent Tracking table](agent-tracking-table.md) — Team-page Excel grid: tenure DERIVED from DOJ ("X Year Y Months Z Days" + 3 buckets); TL Name is an editable reportsTo dropdown (Boss/CenterHead, non-TL rows); no username/password cols; manual metrics per (agentId,date); users.status enum (Active default|Inactive) + Status filter (default Active) + "+ Add Agent" (reportsTo required) + per-row ✕ remove.
- [Sales Performance Dashboard](sales-dashboard.md) — standalone `sales-dashboard` artifact = original 4266-line HTML hosted static (no React), fed one-way from private Google Sheet via PUBLIC `GET /api/sales-dashboard/data` (buildPayload in `@workspace/sales-dashboard-data`); aggregate Overall/ALL rows kept RAW (source is inconsistent with sums); GOOGLE_SHEET_ID env toggles Sheet vs seed, read-fail→502 but bad CELLS are per-row skipped + reported via skippedRows. Adds a "Sales Report" page (rp* fns): conv ratio MUST window sales to the mandays month-set (only 12 FY months) or Convn is wrong; centers data-driven from MD_CENTER_DATA keys.
- [ISS Task Pro mobile app](iss-mobile-app.md) — Expo native client (artifacts/iss-mobile) on SAME api-server + Orval hooks; Bearer token (SESSION_SECRET HMAC) not cookie; setBaseUrl(EXPO_PUBLIC_DOMAIN)+setAuthTokenGetter at module top; queryClient.clear() on signIn/signOut; Orval query opts REQUIRE queryKey.
