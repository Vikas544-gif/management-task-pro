{
  "name": "management-task-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @workspace/api-server run dev",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm run typecheck && pnpm -r run build",
    "db:push": "pnpm --filter @workspace/db run push"
  }
}
